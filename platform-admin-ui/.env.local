# Local development environment variables
# This file is for local overrides and should NOT be committed to git.

# The public URL of the GraphQL API Gateway for the local development environment.
# This points to the API Gateway running locally or in a development cluster.
NEXT_PUBLIC_GRAPHQL_API_URL="http://localhost:4000/graphql"

# Add other local environment variables here if needed
# e.g., NEXTAUTH_SECRET for NextAuth.js if it were used