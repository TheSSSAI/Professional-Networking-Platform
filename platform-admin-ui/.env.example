# Example environment variables for the project.
# Copy this file to .env.local for local development and fill in the values.
# DO NOT commit .env.local to version control.

# The public URL of the GraphQL API Gateway.
# For local development, this is typically http://localhost:4000/graphql
# For staging/production, this will be the public URL of your deployed API gateway.
NEXT_PUBLIC_GRAPHQL_API_URL="http://localhost:4000/graphql"