# Application Configuration
NODE_ENV=development
PORT=50052

# Database (PostgreSQL via Prisma)
# Example for local docker-compose setup
DATABASE_URL="postgresql://user:password@localhost:5432/connectionsdb?schema=public"

# Cache (Redis)
# Example for local docker-compose setup
REDIS_HOST=localhost
REDIS_PORT=6379

# AWS Configuration for SNS Event Publishing
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"

# AWS SNS Topics
# The ARN for the topic where connection-related events are published
CONNECTIONS_SNS_TOPIC_ARN="arn:aws:sns:us-east-1:123456789012:PlatformConnectionsEvents"

# JWT Secret - Should match other services if JWTs are inspected here
# Although gRPC metadata carries the user ID, a guard might validate the token
JWT_SECRET="a_very_secret_and_long_string_for_jwt_in_dev"