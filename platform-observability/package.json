{
  "name": "platform-observability",
  "version": "1.0.0",
  "description": "Configuration-as-code for the professional networking platform's observability stack (Prometheus, Grafana, Alertmanager).",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{json,yml,yaml,md}\"",
    "format:check": "prettier --check \"**/*.{json,yml,yaml,md}\"",
    "validate": "./scripts/validate-configs.sh"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-yaml": "^1.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}